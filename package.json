{
  "name": "cc-session-keepalive",
  "version": "1.0.0",
  "description": "Service to keep Claude Code sessions alive and initiate new sessions",
  "type": "module",
  "author": "Tim Brand <t.brand@gmail.com>",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "version:check": "git describe --tags --abbrev=0 2>/dev/null || echo 'No releases yet'"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}

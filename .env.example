# Required: OAuth token from 'claude setup-token' (valid for 1 year)
CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# Optional: How often to send a keepalive prompt (default: 1 hour)
# Minimum: 30 seconds (30000ms)
PROMPT_INTERVAL_MS=3600000

# Optional: HTTP server port (default: 3000)
PORT=3000

# Optional: Timezone for silence hours (default: Europe/Amsterdam)
TIMEZONE=Europe/Amsterdam

# Optional: Silence hours - no prompts sent during this time window
# Format: HH:MM (24-hour)
SILENCE_START=01:00
SILENCE_END=07:00

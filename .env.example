# Required: OAuth token from 'claude setup-token' (valid for 1 year)
CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# Optional: How often to send a keepalive prompt (default: 4 hours)
# Minimum: 1 hour (3600000ms)
PROMPT_INTERVAL_MS=14400000

# Optional: HTTP server port (default: 3000)
PORT=3000

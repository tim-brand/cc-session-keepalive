# Required: Session key from claude.ai cookies (valid for ~1 month)
# Extract from browser: DevTools → Application → Cookies → claude.ai → sessionKey
SESSION_KEY=sk-ant-sid01-your-session-key-here

# Optional: How often to send a keepalive (default: 1 hour)
# Minimum: 30 seconds (30000ms)
PROMPT_INTERVAL_MS=3600000

# Optional: HTTP server port (default: 3000)
PORT=3000

# Optional: Timezone for silence hours (default: Europe/Amsterdam)
TIMEZONE=Europe/Amsterdam

# Optional: Silence hours - no keepalives sent during this time window
# Format: HH:MM (24-hour)
SILENCE_START=01:00
SILENCE_END=07:00
